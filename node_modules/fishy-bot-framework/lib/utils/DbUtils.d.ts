import { FishyClient } from "..";
export interface db_fetch_options {
    force?: boolean;
    disable_upsert?: boolean;
}
export declare function fetch(client: FishyClient, guild_id: string, options?: db_fetch_options): Promise<any>;
export declare function update(client: FishyClient, guild_id: string, model: any): Promise<void>;
